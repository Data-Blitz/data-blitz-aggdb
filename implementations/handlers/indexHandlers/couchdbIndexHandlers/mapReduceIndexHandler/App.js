
var self;



module.exports = {





    handle:function(aDsl) {
        self.logger.log('info', self.name + ' generating view: ' + payload.length + ' documents');
        self.token = anIndexToken;
        return self.token[0];
    },

    ready:function(aDsl) {
        self = this;

    },

    shutdown: function(aDsl) {

    },
    audit : function(aDsl){

    }
}